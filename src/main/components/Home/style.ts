import styled from "styled-components"

export const Wrapper = styled.section`
  .page {
    background: #ffff;
    border: 5px solid darkred;
    height: 400px;
    width: 600px;
    padding: 10px;
  }
`;
