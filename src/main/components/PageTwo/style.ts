import styled from "styled-components"

export const Wrapper = styled.section`
  .page {
    background: #ffff;
    border: 5px solid darkblue;
    height: 400px;
    width: 600px;
    overflow: scroll;
    padding: 10px;
  }
`;
