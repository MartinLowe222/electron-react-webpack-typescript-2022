import styled from "@emotion/styled"

export const Wrapper = styled.section`
  .page {
    border: 5px solid darkblue;
    height: 400px;
    width: 600px;
    overflow: scroll;
    padding: 10px;
  }
`;
