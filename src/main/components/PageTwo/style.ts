import styled from "styled-components"

export const Wrapper = styled.section`
  .page {
    background: #ffff;
    border: 5px solid darkblue;
    height: 150px;
    width: 200px;
    overflow: scroll;
  }
`;
